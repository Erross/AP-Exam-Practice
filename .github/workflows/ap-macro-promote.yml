name: Promote AP Macroeconomics
'on': [push]
permissions:
  contents: write
jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: subject/ap-macroeconomics
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Promote metadata and regression
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('js/subjects.js'); s=p.read_text()
          anchor='''    id: "ap-macroeconomics",'''
          i=s.index(anchor)
          j=s.index('''  {\n    id: "ap-microeconomics",''', i)
          block=s[i:j]
          assert 'releaseStatus: "draft"' in block
          block=block.replace('releaseStatus: "draft"','releaseStatus: "released"',1)
          p.write_text(s[:i]+block+s[j:])
          t=Path('tests/ap-macroeconomics.test.js'); x=t.read_text()
          assert "assert.equal(subject.releaseStatus,'draft');" in x
          t.write_text(x.replace("assert.equal(subject.releaseStatus,'draft');","assert.equal(subject.releaseStatus,'released');",1))
          PY
          rm .github/workflows/ap-macro-audit.yml
      - run: npm ci
      - name: Final released-mode Macro gate
        run: |
          node --test tests/ap-macroeconomics.test.js
          node tools/subject-release-audit.js --subject ap-macroeconomics --trials 5000 --overlap-trials 5000
          npm run check
      - name: Commit clean release candidate
        run: |
          rm .github/workflows/ap-macro-promote.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'Release AP Macroeconomics for May 2027'
          git push origin HEAD:subject/ap-macroeconomics
