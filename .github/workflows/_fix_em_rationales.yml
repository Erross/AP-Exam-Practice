name: Fix EM Rationales
on:
  push:
    branches: [subject/ap-physics-c-em]
    paths:
      - 'tools/_fix_em_rationales.py'
      - '.github/workflows/_fix_em_rationales.yml'
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: subject/ap-physics-c-em
      - run: python tools/_fix_em_rationales.py
      - run: rm tools/_fix_em_rationales.py .github/workflows/_fix_em_rationales.yml
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data/ap-physics-c-em.js tools/_fix_em_rationales.py .github/workflows/_fix_em_rationales.yml
          git commit -m "Strengthen final E&M rationales"
          git push origin HEAD:subject/ap-physics-c-em
