const fs=require('node:fs');const assert=require('node:assert/strict');const test=require('node:test');const css=fs.readFileSync('course-cards.css','utf8');test('catalog includes how-it-works and credibility strip',()=>{assert.match(css,/HOW IT WORKS/);assert.match(css,/Choose a released course/);assert.match(css,/Check the format/);assert.match(css,/Start timed practice/);assert.match(css,/Current-format settings/);assert.match(css,/Original questions/);assert.match(css,/Progress stays in this browser/);});