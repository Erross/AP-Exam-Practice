// AP Computer Science A U3 — Class Creation. Original AP-style practice.
(function(){"use strict"; const add=window.__APCSA_ADD;
const Q=[
// 3.1 Abstraction and Program Design
{skill:1,code:"3.1",q:`A program models bank accounts. Which class design best uses abstraction?`,correct:`Define an Account class with private balance data and public methods such as deposit and getBalance.`,d:[`Store every account value in unrelated local variables throughout the program.`,`Expose every implementation detail and require client code to update the balance field directly.`,`Represent each account only with comments and no data or behaviors.`],e:`A class provides a useful abstraction by combining related state and behavior behind a clear interface. Private data and public operations let client code use an Account without depending on its internal representation.`},
{skill:2,code:"3.1",q:`A class stores a temperature in Celsius and clients need the Fahrenheit value. Which design best preserves abstraction?`,correct:`Provide a method getFahrenheit() that computes and returns the converted value.`,d:[`Require every client to duplicate the conversion formula using the class's private field.`,`Make the private field inaccessible and provide no operation for conversion.`,`Store the class definition as a String and parse it whenever a client needs a temperature.`],e:`Providing a method for the requested behavior hides the representation and centralizes the conversion logic. Clients can rely on the public operation without duplicating implementation details.`},
{skill:4,code:"3.1",q:`Why is a public method's documented behavior more important to client code than the statements used inside the method?`,correct:`Client code should depend on the method's contract so the implementation can change without requiring client changes.`,d:[`Public methods are not compiled, so only their comments matter.`,`Client code must copy the method body before every call.`,`A method's behavior is irrelevant whenever its implementation uses private variables.`],e:`Procedural abstraction separates interface from implementation. When clients depend on the documented contract rather than internal statements, maintainers can revise the implementation while preserving correct client behavior.`},
// 3.2 Impact of Program Design
{skill:5,code:"3.2",q:`A school app displays a ranking generated from historical discipline data. Which design concern most directly addresses responsible computing?`,correct:`Evaluate whether the historical data or ranking method could encode unfair bias against particular groups.`,d:[`Choose variable names solely to minimize the number of characters typed.`,`Replace every loop with a longer sequence of repeated statements.`,`Make all fields public so users can modify rankings directly.`],e:`Responsible program design includes considering how data selection and algorithmic decisions can create or perpetuate unfair outcomes. Historical records may reflect prior bias, so evaluating impact is a substantive design responsibility.`},
{skill:5,code:"3.2",q:`An app collects precise location data even though it only needs a user's state. Which change best follows data-minimization principles?`,correct:`Collect only the less precise location information needed for the stated feature.`,d:[`Collect additional unrelated personal data in case it becomes useful later.`,`Publish the precise data so users can see what was collected.`,`Store the precise data forever because deleting data can complicate implementation.`],e:`Data minimization means limiting collection to information necessary for the intended purpose. Avoiding unnecessary precise location data reduces privacy exposure while still supporting a state-level feature.`},
{skill:1,code:"3.2",q:`A team is deciding whether to make an accessibility setting part of the initial class design or add it later. What is the strongest design reason to consider it early?`,correct:`Early design can make the interface and data model support a broader range of users without costly structural changes later.`,d:[`Accessibility concerns are unrelated to program design.`,`Java prevents accessibility features from being added after compilation.`,`Adding accessibility always reduces the correctness of an algorithm.`],e:`Program design choices affect who can effectively use a system and how difficult later changes will be. Considering accessibility early can shape interfaces and abstractions in ways that support users more inclusively.`},
// 3.3 Anatomy of a Class
{skill:3,code:"3.3",q:`In a typical Java class, which declaration represents an instance variable?`,correct:`private int score;`,d:[`public void score() { }`,`score = 10;`,`return score;`],e:`An instance variable is a field declared in the class outside individual methods and constructors. The declaration private int score; defines per-object state rather than a method, assignment statement, or return statement.`},
{skill:2,code:"3.3",q:`Which class skeleton correctly declares a private String field named name?`,correct:`class Student { private String name; }`,d:[`class Student { private name String; }`,`class Student(String name) { }`,`private class Student = String name;`],e:`Java field declarations place the access modifier, type, and identifier in that order inside the class body. The correct skeleton therefore declares private String name; as a field of Student.`},
{skill:4,code:"3.3",q:`What is the main purpose of declaring instance variables private?`,correct:`It restricts direct client access so the class can control how its internal state is read or changed.`,d:[`It prevents methods of the same class from accessing the variables.`,`It causes a new variable to be created every time a method is called.`,`It makes the variables constants that can never change.`],e:`Private access supports encapsulation by limiting field access to code within the class. Public methods can then enforce invariants or provide controlled views of the object's state.`},
// 3.4 Constructors
{skill:2,code:"3.4",q:`For class Point, which declaration is a valid constructor?`,correct:`public Point(int x, int y) { /* initialize fields */ }`,d:[`public void Point(int x, int y) { }`,`public int Point(int x, int y) { return x; }`,`constructor Point = (int x, int y);`],e:`A Java constructor has the same name as the class and has no return type, not even void. The valid declaration therefore begins public Point(...) and can initialize the new object's fields.`},
{skill:3,code:"3.4",q:`What happens when new Counter(5) is evaluated and Counter has a matching constructor?`,correct:`Memory is allocated for a Counter object, its constructor initializes the object, and a reference to it is produced.`,d:[`The constructor returns the primitive int 5.`,`A class variable named new is assigned the value 5.`,`The constructor is skipped because constructors run only after the first method call.`],e:`The new expression creates an object and invokes a compatible constructor to establish its initial state. The expression evaluates to a reference to that newly created object.`},
{skill:3,code:"3.4",q:`A class declares only public Box(int size). Which statement about new Box() is correct?`,correct:`It does not compile because no zero-argument constructor is available.`,d:[`Java automatically supplies a zero-argument constructor even when another constructor is declared.`,`It creates a Box and passes the default int value 0 to the one-argument constructor.`,`It compiles only if size is a public instance variable.`],e:`A default zero-argument constructor is supplied only when the class declares no constructors. Once Box(int) is explicitly declared, new Box() requires a separately declared zero-argument constructor.`},
// 3.5 Methods: How to Write Them
{skill:2,code:"3.5",q:`Which method correctly returns the square of its int parameter n?`,correct:`public int square(int n) { return n * n; }`,d:[`public void square(int n) { return n * n; }`,`public int square { return n * n; }`,`public int square(int n) { n * n; }`],e:`The method declares an int return type, accepts an int parameter, and returns an int expression on every execution path. A void method cannot return a value, and a nonvoid method needs a valid return statement.`},
{skill:3,code:"3.5",q:`Why is this method invalid? public int f(int x) { if (x > 0) return x; }`,correct:`There is an execution path for x <= 0 that reaches the end without returning an int value.`,d:[`An int method is not allowed to use an if statement.`,`Every parameter of an int method must be double.`,`A return statement may appear only in a constructor.`],e:`A nonvoid method must return a value compatible with its declared type on every possible execution path. When x <= 0, this method reaches its closing brace without returning an int.`},
{skill:4,code:"3.5",q:`A method parameter is named radius. What is the parameter's role when the method is called?`,correct:`It is a local variable initialized with the corresponding argument value for that invocation.`,d:[`It permanently replaces every variable named radius in the program.`,`It is automatically a public instance variable of the class.`,`It can be used only in comments and not in executable statements.`],e:`Parameters are local variables of a method invocation. Each receives the corresponding argument value when the call begins and is in scope according to the method's parameter and block rules.`},
// 3.6 Passing and Returning References
{skill:3,code:"3.6",q:`A method receives an ArrayList reference as a parameter and calls list.add("x"). What can happen to the caller's object?`,correct:`The shared ArrayList object can be modified, so the caller can observe the added element.`,d:[`The caller's ArrayList can never change because all parameters create deep copies.`,`The parameter becomes a primitive int inside the method.`,`Calling add automatically reassigns the caller's variable to null.`],e:`Java passes a copy of the reference value. The parameter and caller variable can therefore refer to the same mutable object, allowing method calls through the parameter to modify that shared object's state.`},
{skill:3,code:"3.6",q:`A method executes p = new Point(0, 0); where p is a reference parameter. What happens to the caller's reference variable solely because of this assignment?`,correct:`The caller's variable is not reassigned; only the method's local copy of the reference changes.`,d:[`The caller's variable automatically refers to the new Point.`,`The original caller variable becomes an int pair.`,`The assignment changes the class definition of Point.`],e:`Argument passing copies the reference value into the parameter. Reassigning the parameter changes that local parameter variable, not the caller's separate reference variable, although object mutations through a shared reference can be visible.`},
{skill:2,code:"3.6",q:`Which method can return a reference to a Student object stored in an instance variable current?`,correct:`public Student getCurrent() { return current; }`,d:[`public void getCurrent() { return current; }`,`public int getCurrent() { return current; }`,`public Student getCurrent { current; }`],e:`The method's declared return type must be compatible with the reference being returned. Returning current from a Student-returning method correctly passes a reference value back to the caller.`},
// 3.7 Class Variables and Methods
{skill:3,code:"3.7",q:`A field is declared private static int count;. What is true of count?`,correct:`One class-level count is shared by all instances of the class.`,d:[`Each instance automatically has an independent count field.`,`count can be accessed only through newly created local variables.`,`static makes count a String rather than an int.`],e:`A static field belongs to the class rather than to each individual instance. All instances therefore observe the same class-level storage for that field, subject to access control.`},
{skill:2,code:"3.7",q:`Which declaration defines a class method that can be called as Utility.reset()?`,correct:`public static void reset() { /* code */ }`,d:[`public void static reset() { }`,`public Utility reset static() { }`,`private int reset = static;`],e:`A class method is declared with static. The declaration public static void reset() provides a no-argument class method whose invocation can use the class name Utility.`},
{skill:3,code:"3.7",q:`Why can a static method not directly use an instance variable without an object reference?`,correct:`A static method belongs to the class and has no particular current instance whose instance field should be used.`,d:[`Instance variables exist only while constructors are executing.`,`Static methods are forbidden from reading any variables.`,`Instance fields are always private constants.`],e:`Instance variables belong to particular objects, while a static method executes in class context without an implicit receiver object. It therefore needs an explicit object reference to access per-instance state.`},
// 3.8 Scope and Access
{skill:3,code:"3.8",q:`A variable is declared inside the body of an if statement. Where can that local variable normally be referenced?`,correct:`From its declaration through the end of that enclosing block.`,d:[`Anywhere in every class in the program.`,`Only before its declaration.`,`Only in constructors, regardless of where it was declared.`],e:`Java block scope limits a local variable to the block in which it is declared, beginning at its declaration. Once execution and source context leave that block, the identifier is no longer in scope.`},
{skill:4,code:"3.8",q:`A client class tries to access obj.secret where secret is a private instance variable of obj's class. What should the client generally do instead?`,correct:`Use an appropriate public method provided by the class, if its interface permits that access.`,d:[`Change secret to a local variable inside the client.`,`Prefix the expression with static to bypass private access.`,`Use a comment containing the value of secret.`],e:`Private fields are intentionally hidden from client code. If external access is appropriate, the defining class should expose controlled behavior through public methods rather than requiring clients to violate encapsulation.`},
{skill:3,code:"3.8",q:`A method declares int x = 2; and a nested block later declares int y = x + 1;. Which statement is true inside that nested block?`,correct:`Both x and y are in scope there.`,d:[`Only y is in scope because entering a nested block erases outer locals.`,`Neither variable is in scope until the method returns.`,`x becomes a public instance variable when y is declared.`],e:`A nested block can access local variables from enclosing scopes when their declarations precede the block. The newly declared y is also in scope from its own declaration through the end of that nested block.`},
// 3.9 this Keyword
{skill:2,code:"3.9",q:`A constructor has parameter int size and instance variable private int size;. Which statement correctly assigns the parameter to the instance variable?`,correct:`this.size = size;`,d:[`size.this = this;`,`this = size;`,`size = this.size = this;`],e:`this refers to the current object. Therefore this.size selects the instance variable while the unqualified size refers to the parameter, allowing the parameter value to initialize the object's field.`},
{skill:3,code:"3.9",q:`Inside an instance method, what does this refer to?`,correct:`The object on which the method was invoked.`,d:[`The class's source file as a String.`,`The most recently declared local variable.`,`Every object of the class simultaneously.`],e:`Within an instance method or constructor, this is the reference to the current receiver object. It can be used to distinguish instance fields or pass the current object where a reference is required.`},
{skill:3,code:"3.9",q:`Suppose a method parameter and an instance variable are both named value. What does return value; refer to if no local variable shadows the parameter?`,correct:`The parameter named value.`,d:[`The instance variable, because fields always have priority.`,`The class variable even if no static field exists.`,`A compile-time error because duplicate names are never allowed in different scopes.`],e:`An unqualified identifier resolves to the nearest applicable declaration. A parameter named value shadows an instance variable of the same name; this.value would explicitly select the instance field.`}
];
Q.forEach((x,i)=>add({id:`apcsa-u3-${String(i+1).padStart(2,"0")}`,unit:"U3",topicCode:x.code,skill:x.skill,q:x.q,correct:x.correct,distractors:x.d,e:x.e}));
})();
