// AP Computer Science A U2 — Selection and Iteration. Original AP-style practice.
(function(){"use strict"; const add=window.__APCSA_ADD;
const Q=[
// 2.1 Algorithms with Selection and Repetition
{skill:1,code:"2.1",q:`A program processes temperatures until a sentinel value is entered and counts how many temperatures are below freezing. Which design uses the required algorithmic building blocks?`,correct:`Repeat reading values until the sentinel; for each ordinary value, use a selection to increment the count when the value is below zero.`,d:[`Read exactly one value and increment the count without testing it.`,`Sort every possible temperature before accepting input.`,`Use selection once before any values are read and avoid repetition.`],e:`The task needs repetition for an unknown number of inputs and selection for deciding which inputs contribute to the count. Sequencing places reading, testing, and updating in the correct order.`},
{skill:2,code:"2.1",q:`Which pseudocode correctly counts positive values in a sequence?`,correct:`Set count to 0; for each value, if value > 0 then increase count by 1.`,d:[`Set count to 1; for each value, decrease count when value > 0.`,`If the first value is positive, stop before examining the remaining values.`,`For each value, increase the value itself and never update count.`],e:`A counting algorithm initializes an accumulator and conditionally updates it once for each qualifying element. The alternatives either start with the wrong count or fail to process the complete sequence.`},
{skill:4,code:"2.1",q:`Why can changing the order of selection and repetition steps change an algorithm's result?`,correct:`The order determines when decisions are made relative to each repeated input or state update.`,d:[`Java ignores all statements inside loops when an if statement exists.`,`Selection and repetition are interchangeable keywords with identical behavior.`,`The compiler automatically reorders control structures to produce the intended answer.`],e:`Sequencing is part of an algorithm. Testing once outside a loop can differ fundamentally from testing each iteration, so moving a selection relative to repetition can change which values are processed.`},
// 2.2 Boolean Expressions
{skill:3,code:"2.2",q:`Given int x = 7; int y = 10;, what is the value of x < y?`,correct:`true`,d:[`false`,`7`,`10`],e:`The relational expression compares the two int values and evaluates to a boolean. Because 7 is less than 10, x < y evaluates to true rather than a numeric value.`},
{skill:2,code:"2.2",q:`Which expression is true exactly when score is at least 60?`,correct:`score >= 60`,d:[`score > 60`,`score == 60`,`score <= 60`],e:`The phrase “at least 60” includes 60 itself and every greater value, which is represented by >=. The strict > operator incorrectly excludes the boundary value 60.`},
{skill:3,code:"2.2",q:`Given boolean ready = false; what is the value of !ready?`,correct:`true`,d:[`false`,`0`,`The expression cannot compile because ! works only on numbers.`],e:`The logical NOT operator negates a boolean value. Since ready is false, !ready evaluates to true; Java does not use numeric 0 and 1 as boolean values.`},
// 2.3 if Statements
{skill:2,code:"2.3",q:`Which code prints "adult" when age is 18 or greater and otherwise prints nothing?`,correct:`if (age >= 18) { System.out.println("adult"); }`,d:[`if (age < 18) { System.out.println("adult"); }`,`if age >= 18 System.out.println("adult");`,`if (age = 18) { System.out.println("adult"); }`],e:`The if condition must be a boolean expression and the desired boundary is age >= 18. Assignment with = is not the required comparison, and reversing the relation selects the wrong ages.`},
{skill:3,code:"2.3",q:`What is printed?
int x = 4;
if (x % 2 == 0) { System.out.print("E"); }
System.out.print("N");`,correct:`EN`,d:[`E`,`N`,`Nothing is printed.`],e:`Because 4 % 2 is 0, the if body prints E. The statement after the if is not conditional, so it then prints N, producing EN in sequence.`},
{skill:4,code:"2.3",q:`For if (condition) statement;, when is statement executed?`,correct:`Only when condition evaluates to true.`,d:[`Only when condition evaluates to false.`,`Exactly twice whenever condition is boolean.`,`Before condition is evaluated.`],e:`An if statement evaluates its boolean condition first and executes its controlled statement only on the true branch. Without an else, a false condition simply skips that body.`},
// 2.4 Nested if Statements
{skill:2,code:"2.4",q:`Which structure best represents “if the account is active, then if the balance is negative, print a warning”?`,correct:`if (active) { if (balance < 0) { System.out.println("warning"); } }`,d:[`if (balance < 0) { active = false; }`,`if (active && balance >= 0) { System.out.println("warning"); }`,`if (active) { balance < 0; System.out.println("warning"); }`],e:`The second decision is required only after the first condition succeeds, so a nested if directly models the specification. The warning therefore requires both active and a negative balance.`},
{skill:3,code:"2.4",q:`What is printed when x is 8?
if (x > 0) {
  if (x < 5) System.out.print("A");
  else System.out.print("B");
}`,correct:`B`,d:[`A`,`AB`,`Nothing is printed.`],e:`The outer condition is true because 8 > 0. The nested condition 8 < 5 is false, so its associated else branch prints B and no other print statement executes.`},
{skill:3,code:"2.4",q:`In nested if statements without braces, an else is associated with which if by default?`,correct:`The nearest preceding unmatched if.`,d:[`The first if in the method.`,`Every preceding if at the same time.`,`No if; an else always requires braces.`],e:`Java's dangling-else rule associates an else with the nearest unmatched if. Braces are often used for clarity, but they are not required merely to make an else legal.`},
// 2.5 Compound Boolean Expressions
{skill:2,code:"2.5",q:`Which expression is true when n is between 1 and 10 inclusive?`,correct:`n >= 1 && n <= 10`,d:[`n >= 1 || n <= 10`,`n < 1 && n > 10`,`n == 1 && n == 10`],e:`Both boundary conditions must hold simultaneously, so logical AND is required. Using OR would make nearly every integer satisfy at least one side of the expression.`},
{skill:3,code:"2.5",q:`Given boolean a = true; boolean b = false;, what is a && !b?`,correct:`true`,d:[`false`,`0`,`The expression has no value because && cannot use booleans.`],e:`!b negates false to true. The AND of true and true is true, so the complete compound boolean expression evaluates to true.`},
{skill:3,code:"2.5",q:`For expression p || q, when is the result false?`,correct:`Only when both p and q are false.`,d:[`Whenever p is true.`,`Whenever exactly one operand is true.`,`Only when both p and q are true.`],e:`Logical OR is true if at least one operand is true. Therefore the only case in which p || q is false is when neither operand is true.`},
// 2.6 Comparing Boolean Expressions
{skill:2,code:"2.6",q:`Which expression is equivalent to !(x > 5)?`,correct:`x <= 5`,d:[`x >= 5`,`x < 5`,`x == 5`],e:`Negating “x is greater than 5” includes every value less than or equal to 5. The boundary 5 must be included because x > 5 is false there.`},
{skill:3,code:"2.6",q:`Which expression is equivalent to !(a && b) by De Morgan's law?`,correct:`!a || !b`,d:[`!a && !b`,`a || b`,`a && !b`],e:`De Morgan's law negates each operand and changes AND to OR. Therefore not both a and b is equivalent to at least one of a or b being false.`},
{skill:3,code:"2.6",q:`If x is an int, which expression is equivalent to x < 3 || x > 9?`,correct:`!(x >= 3 && x <= 9)`,d:[`!(x < 3 && x > 9)`,`x >= 3 && x <= 9`,`!(x <= 3 || x >= 9)`],e:`The original condition describes values outside the inclusive interval 3 through 9. Negating the condition for being inside that interval gives the equivalent expression shown.`},
// 2.7 while Loops
{skill:2,code:"2.7",q:`Which loop prints 1, 2, and 3 exactly once each?`,correct:`int i = 1; while (i <= 3) { System.out.print(i); i++; }`,d:[`int i = 1; while (i <= 3) { System.out.print(i); }`,`int i = 1; while (i > 3) { System.out.print(i); i++; }`,`int i = 3; while (i <= 3) { i++; System.out.print(i); }`],e:`The loop starts at 1, prints before incrementing, and continues while i is at most 3. Updating i each iteration prevents an infinite loop and yields exactly the requested values.`},
{skill:3,code:"2.7",q:`What is sum after this code?
int n = 3;
int sum = 0;
while (n > 0) { sum += n; n--; }`,correct:`6`,d:[`3`,`5`,`7`],e:`The loop adds 3, then 2, then 1 before n reaches 0 and the condition becomes false. The accumulated total is therefore 3 + 2 + 1 = 6.`},
{skill:4,code:"2.7",q:`Which condition most directly causes an infinite while loop?`,correct:`The loop condition stays true and the body never changes any state that can make it false.`,d:[`The condition is false before the first iteration.`,`The body executes a finite number of arithmetic statements.`,`The loop control variable is initialized before the loop.`],e:`A while loop continues as long as its condition remains true. If no executed statement can move the program toward a false condition, the loop can repeat indefinitely.`},
// 2.8 for Loops
{skill:2,code:"2.8",q:`Which for loop executes its body exactly five times with i taking values 0 through 4?`,correct:`for (int i = 0; i < 5; i++)`,d:[`for (int i = 0; i <= 5; i++)`,`for (int i = 5; i < 5; i++)`,`for (int i = 0; i < 5; i--)`],e:`Initialization gives i = 0, the strict test i < 5 permits values 0 through 4, and i++ advances toward termination. That produces exactly five iterations.`},
{skill:3,code:"2.8",q:`What is printed?
for (int i = 2; i <= 6; i += 2) { System.out.print(i + " "); }`,correct:`2 4 6`,d:[`2 3 4 5 6`,`2 4`,`4 6 8`],e:`The control variable starts at 2 and increases by 2 after each iteration. Values 2, 4, and 6 satisfy i <= 6; the next value 8 stops the loop.`},
{skill:3,code:"2.8",q:`Which while-loop initialization and update are equivalent to for (int k = 3; k > 0; k--)?`,correct:`int k = 3; while (k > 0) { /* body */ k--; }`,d:[`int k = 0; while (k > 3) { /* body */ k--; }`,`int k = 3; while (k > 0) { /* body */ k++; }`,`int k = 3; while (k >= 0) { /* body */ k--; }`],e:`An equivalent while loop preserves the same initialization, continuation condition, and update. Changing any of those elements changes the iteration count or can prevent termination.`},
// 2.9 Implementing Selection and Iteration Algorithms
{skill:2,code:"2.9",q:`Which pattern correctly finds the number of even integers from 1 through n?`,correct:`Initialize count to 0; loop i from 1 through n; increment count when i % 2 == 0.`,d:[`Initialize count to n; decrement count for every even i.`,`Loop only while i is even, starting with i = 1.`,`Increment count for every i without checking parity.`],e:`The algorithm must examine each candidate value and selectively update an accumulator for even values. The modulus test identifies divisibility by 2 while the loop covers the entire range.`},
{skill:3,code:"2.9",q:`What value is stored in count?
int count = 0;
for (int i = 1; i <= 8; i++) {
  if (i % 3 == 0) count++;
}`,correct:`2`,d:[`1`,`3`,`8`],e:`Among the integers 1 through 8, exactly 3 and 6 are divisible by 3. The conditional increment therefore executes twice, leaving count equal to 2.`},
{skill:4,code:"2.9",q:`A loop is intended to find the first value greater than 100 in an increasing sequence. Why can it stop as soon as that value is found?`,correct:`Once the first qualifying value is identified, later values cannot change which value was first.`,d:[`Java requires every search loop to stop after one match.`,`Values after the match are automatically deleted from the sequence.`,`A loop cannot legally examine values greater than 100.`],e:`The specification asks only for the first qualifying element. Because the sequence is processed in order, once that element is found, later processing cannot produce an earlier match and is unnecessary.`},
// 2.10 Implementing String Algorithms
{skill:2,code:"2.10",q:`Which loop header can examine every character index of String s from left to right?`,correct:`for (int i = 0; i < s.length(); i++)`,d:[`for (int i = 1; i <= s.length(); i++)`,`for (int i = 0; i <= s.length(); i++)`,`for (int i = s.length(); i > 0; i++)`],e:`Valid String character indices run from 0 through length() - 1. Starting at 0 and continuing while i < length() visits each valid index exactly once.`},
{skill:3,code:"2.10",q:`What is count after this code?
String s = "banana";
int count = 0;
for (int i = 0; i < s.length(); i++) {
  if (s.substring(i, i + 1).equals("a")) count++;
}`,correct:`3`,d:[`2`,`4`,`6`],e:`The loop examines each one-character substring. The String "banana" contains the character "a" at indices 1, 3, and 5, so the conditional increment occurs three times.`},
{skill:3,code:"2.10",q:`Why is s.substring(i, i + 1) safe in a loop whose condition is i < s.length()?`,correct:`The largest i is length() - 1, making the exclusive end index i + 1 equal to length(), which is valid.`,d:[`substring permits every end index greater than length().`,`The method ignores i and always returns the first character.`,`String indices begin at 1, so i + 1 is the actual character index.`],e:`substring uses an inclusive beginning and exclusive end. For the final character, begin is length()-1 and end is length(), which is exactly the valid boundary for the exclusive endpoint.`},
// 2.11 Nested Iteration
{skill:2,code:"2.11",q:`Which nested loop executes the inner statement 12 times?`,correct:`for (int r = 0; r < 3; r++) { for (int c = 0; c < 4; c++) { /* statement */ } }`,d:[`for (int r = 0; r < 3; r++) { for (int c = 0; c < 3; c++) { /* statement */ } }`,`for (int r = 0; r < 4; r++) { /* statement */ }`,`for (int r = 0; r < 3; r++) { for (int c = r; c < 3; c++) { /* statement */ } }`],e:`For each of 3 outer iterations, the inner loop runs 4 times, giving 3 × 4 = 12 executions. The other structures have different iteration counts.`},
{skill:3,code:"2.11",q:`How many times is count++ executed?
for (int i = 0; i < 4; i++)
  for (int j = 0; j < i; j++)
    count++;`,correct:`6`,d:[`4`,`8`,`16`],e:`The inner loop runs 0 times for i=0, then 1, 2, and 3 times. The total is 0 + 1 + 2 + 3 = 6 executions of count++.`},
{skill:4,code:"2.11",q:`In a rectangular nested traversal with r rows and c columns, why does a complete row-by-column scan perform r * c element visits?`,correct:`Each of the r outer iterations performs c inner visits.`,d:[`The inner loop runs only once for all rows combined.`,`The loops execute in parallel so their counts are added.`,`Nested loops automatically skip the first row and column.`],e:`A full nested traversal repeats the complete c-iteration inner loop for each of r outer iterations. Multiplying the per-row visits by the number of rows gives r*c total visits.`},
// 2.12 Informal Run-Time Analysis
{skill:4,code:"2.12",q:`Two algorithms process n elements. Algorithm A examines each element once; Algorithm B compares every element with every element. Which statement is most accurate for large n?`,correct:`Algorithm B's work grows much faster because its nested comparisons are proportional to n squared.`,d:[`Both algorithms perform approximately the same number of operations for every n.`,`Algorithm A grows faster because one loop is always slower than two loops.`,`Algorithm B performs a constant number of comparisons independent of n.`],e:`A single full traversal grows roughly in proportion to n, while two n-length nested traversals grow roughly in proportion to n². The quadratic growth dominates as n becomes large.`},
{skill:3,code:"2.12",q:`If a loop body executes once for each integer i from 0 through n - 1, how does the number of executions grow as n doubles?`,correct:`It approximately doubles.`,d:[`It stays constant.`,`It approximately quadruples.`,`It becomes half as large.`],e:`The loop performs n iterations, so its work is proportional to n. Replacing n with 2n therefore changes the number of body executions from n to 2n.`},
{skill:3,code:"2.12",q:`A nested loop executes n iterations outside and n iterations inside for every outer iteration. If n changes from 100 to 200, approximately how does the number of inner-body executions change?`,correct:`It becomes four times as large.`,d:[`It doubles.`,`It stays the same.`,`It becomes eight times as large.`],e:`The body executes n² times. Changing n from 100 to 200 multiplies n by 2, so n² is multiplied by 2² = 4.`}
];
Q.forEach((x,i)=>add({id:`apcsa-u2-${String(i+1).padStart(2,"0")}`,unit:"U2",topicCode:x.code,skill:x.skill,q:x.q,correct:x.correct,distractors:x.d,e:x.e}));
})();
